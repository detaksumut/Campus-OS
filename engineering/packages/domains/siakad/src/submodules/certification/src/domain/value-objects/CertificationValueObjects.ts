export class ApplicationId {
  constructor(private readonly id: string) {}
  getValue(): string { return this.id; }
}

export class ProgramId {
  constructor(private readonly id: string) {}
  getValue(): string { return this.id; }
}

export class ExamId {
  constructor(private readonly id: string) {}
  getValue(): string { return this.id; }
}

export class InterviewId {
  constructor(private readonly id: string) {}
  getValue(): string { return this.id; }
}

export class CertificateId {
  constructor(private readonly id: string) {}
  getValue(): string { return this.id; }
}

export class CandidateId {
  constructor(private readonly id: string) {}
  getValue(): string { return this.id; }
}

export class AssessorId {
  constructor(private readonly id: string) {}
  getValue(): string { return this.id; }
}
