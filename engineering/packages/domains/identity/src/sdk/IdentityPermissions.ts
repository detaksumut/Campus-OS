export enum IdentityPermissions {
    // Publication
    JournalSubmit = 'journal.submit',
    JournalReview = 'journal.review',
    JournalPublish = 'journal.publish',

    // Academic
    CourseView = 'course.view',
    CourseEdit = 'course.edit',
    ExamTake = 'exam.take',

    // Library
    BookBorrow = 'book.borrow',
    BookReturn = 'book.return',
}
