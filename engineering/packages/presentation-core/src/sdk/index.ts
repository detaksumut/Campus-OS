export function definePlugin(config: any) {
  return config;
}

export function definePage(config: any) {
  return config;
}

export function defineWidget(config: any) {
  return config;
}

export function defineForm(config: any) {
  return config;
}
